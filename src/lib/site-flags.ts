// ─── Site-wide feature flags ───────────────────────────────────────────────
// To pause orders:  set ORDERS_PAUSED = true  → push → live in ~2 min
// To resume orders: set ORDERS_PAUSED = false → push → live in ~2 min

export const ORDERS_PAUSED = true;
