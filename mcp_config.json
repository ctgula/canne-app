{
  "version": 1,
  "servers": {
    "supabase": {
      "type": "supabase",
      "config": {
        "url": "${NEXT_PUBLIC_SUPABASE_URL}",
        "key": "${NEXT_PUBLIC_SUPABASE_ANON_KEY}",
        "region": "us-east-1"
      }
    }
  }
}
